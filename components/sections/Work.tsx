import ProjectRotunda from "@/components/work/ProjectRotunda";

/**
 * Home work teaser — Lucas rotunda with link to full /work page.
 */
export default function Work() {
  return (
    <section id="work">
      <ProjectRotunda showAllLink />
    </section>
  );
}
